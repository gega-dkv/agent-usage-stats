{
  "name": "@agent-usage/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell for Agent Usage Stats",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "typecheck": "node -e \"console.log('Desktop typecheck: run cargo check in src-tauri when Rust toolchain is installed')\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3"
  }
}
